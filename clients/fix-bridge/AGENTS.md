# FIX bridge instructions

Expose standard local FIX/TCP and tunnel complete raw FIX messages over WSS. Do not rewrite sequence numbers or business fields silently. Support partial TCP reads and bounded reconnect/backpressure.
