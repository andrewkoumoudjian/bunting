# scenario-engine instructions

Compile validated scenarios into deterministic scheduled commands and agent configuration. Persist seed derivation versions and keep conditional rules bounded and auditable.
