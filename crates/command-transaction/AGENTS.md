# Command-transaction instructions

Keep orchestration sans-I/O and deterministic. Restore public book state through `bunting-orderbook`, consume upstream results without re-deciding matches, project private accounting from explicit trades and ownership, commit origin state before cache writes, and treat cache failures as recovery conditions.
