# test-fixtures instructions

Store small, licensed, deterministic fixtures and golden messages. Do not hide test logic in fixtures or copy large upstream datasets into the production dependency graph.
