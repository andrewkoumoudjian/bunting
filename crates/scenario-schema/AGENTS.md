# scenario-schema instructions

Define versioned, canonical scenario documents with strict validation and deterministic hashing. Published versions are immutable. Avoid executable arbitrary code in scenario definitions.
