# ADR instructions

Each ADR must include status, context, decision, consequences, rejected alternatives, validation, operational impact, security impact, and references. Amend decisions with a new ADR rather than silently rewriting accepted history.
