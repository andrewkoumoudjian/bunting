# Unified engine parity matrix

This matrix combines observed reference capabilities with the current Rust implementation. `Full` means the cited source or current code demonstrably covers the row; `partial` means only a subset or adjacent primitive exists; `none` means no coverage; `unresolved` means the reference evidence is insufficient. RIT entries point to stable IDs in [`market-feature-ledger.md`](market-feature-ledger.md); NBC, RITC, QUARCC and OrderBook-rs claims use the pinned audits and port notes listed below.

The target is one `bunting-engine`. Compatibility profiles may alter validation, timing, agents, and output shape, but they do not select a different authoritative kernel.

| Capability | OrderBook-rs 0.10.3 | RIT/RTD evidence | NBC evidence/current Rust | `ritc_mm` evidence | QUARCC evidence/current Rust | Current Bunting | Unified owner and gap |
|---|---|---|---|---|---|---|---|
| Limit/market matching and partial fills | Full | Full surface; hidden priority unresolved (`0007`) | Limit matching is translated into a separate `NbcOrderBook`; external trace parity unresolved | Adapter submits to RIT | Participant submit/report contracts only | Adapter uses OrderBook-rs for limit/market | `bunting-engine/matching`; replace NBC production matcher with compatibility preprocessing/differential oracle |
| IOC, FOK and post-only | Full | Not exposed by extracted RIT surface | Unresolved | Not observed | TIF contract includes variants but venue behavior external | Upstream types are re-exported; command path incomplete | Expose complete upstream operations and regression tests |
| Iceberg/reserve, pegged, trailing-stop, market-to-limit | Full | Not observed | Unresolved | Not observed | Not venue-side | Upstream surface available but not composed | Preserve through matching facade and snapshots |
| Price-time priority | Full | Unresolved | JAR bytecode evidence and translated matcher slice exist; external parity incomplete | Not venue-side | Not venue-side | Upstream default | One upstream matcher; NBC differences become proven policy or narrowly documented extension |
| Cancel and mass cancel | Full | Single/bulk/expression cancel (`0009`) | Cancel slice implemented separately | Quote planner requires cancel | Cancel/cancel-all intent | Single cancel command path | Unified commands plus RIT predicate and NBC output profiles |
| Replace | Full upstream API | No direct REST/API replace observed; OTC update is separate | Unresolved | Desired/live replace behavior | Explicit participant replace | Not in canonical command path | Add canonical replace while preserving priority policy |
| STP | Full | Unresolved | Unresolved | Participant strategy avoids self-cross only | Participant risk only | Upstream available | Matching policy and parity tests; no RIT/NBC equivalence claim |
| Fees/rebates | Full hooks | Per-unit/per-trade/percentage fee and limit rebate (`0024`) | Scenario fields possible; formula unresolved | Strategy P&L diagnostics | Participant projections | Upstream fee hooks plus basic ledger | Engine fee policy and exact ledger postings |
| Kill switch/halt/drain | Full book controls | Stop-loss/order-book controls, game pause/stop (`0002`,`0028`) | Run status exists; exact halt behavior unresolved | Strategy stop/cancel planning | Participant kill switch contract | Risk run control and upstream kill switch primitives | Separate venue halt from participant routing kill switch |
| GTD/DAY/expiry | Full host-driven expiry | Tender/lease/order expiry fields; order TIF unresolved | Step scheduling primitives | Stale quote cancellation | TIF DTOs | Upstream available, command path incomplete | Engine clock drives all expiries deterministically |
| L1/L2 and iterators | Full | Raw and aggregated depth (`0015`-`0017`) | Market WebSocket contract; current book slice | Consumes book | Consumes market data | Snapshot exposes visible levels only | Unified market-data projections and bounded subscriptions |
| Snapshot/restore/replay | Full book snapshot/replay | Not proved (`0060`) | Bunting-added config hashes; full state snapshot absent | Model warmup state proposed | Portable snapshot proposed, not implemented | Book snapshot and origin recovery vertical slice | Snapshot all run, agents, ledger, risk, event, compatibility and queue state |
| Metrics/impact/analytics | Full metrics, iterators and impact | Market VWAP/impact and charts (`0019`,`0055`) | Leaderboard/scoring surfaces, formulas unresolved | Volatility/imbalance/quote analytics | Participant diagnostics | Upstream impact not fully exposed | Keep book analytics in matcher facade; strategy analytics in built-in agents |
| Run lifecycle and logical clock | Host-supplied time only | Period/tick/pause/stop (`0001`-`0003`) | Deterministic step run kernel partially implemented | Polling cadence is participant-side | No venue clock | No unified run kernel | `run/clock/scheduling`, with wall-clock and explicit-step profiles |
| Scenario configuration/provenance | None | Rich client-side case parameters (`0003`-`0006`,`0046`) | Strict config/provenance hashes implemented | Strategy config | Execution config | Split across packages | One versioned engine config with compatibility sections |
| `DONE` step synchronization | None | Not observed | Observed external contract; not integrated into current engine | Adapter concern only | None | None | NBC adapter command over the unified scheduler |
| Virtual trader populations | None | Trader types/endowments observed; formulas unresolved (`0042`) | Scenario populations observed; agent formulas under translation | Model can become built-in market maker | External strategies only | None | Engine-owned deterministic agents with provenance and seeds |
| Simulated liquidity and path following | Matching only | `IsFollowPath`/order size evidence (`0041`) | Simulated populations/stress expected; incomplete | Endogenous quote generation models | Simulated gateway is participant-side | None | Built-in liquidity agents submitting ordinary engine commands |
| Flash crash/stress/special events | None | News/modules/events exist; formulas unresolved | NBC target and scenario evidence; incomplete | Stress response/liquidity withdrawal concepts | No venue authority | None | Engine event schedule and agent policy; prove each profile separately |
| Public/private participant views | Per-user order index only | Extensive REST/RTD/account views (`0020`-`0023`,`0050`) | Public/private WebSockets observed | Consumes private reports | Maintains local reports | Basic origin owned orders and ledger | Canonical projections with authorization and committed sequence |
| Positions, cash, BP and NLV | None | Full exposed state; formulas unresolved (`0021`-`0023`) | Limits/results surfaces, formulas unresolved | Consumes external P&L | Position DTOs only | Basic exact ledger/account reservations | Extend exact ledger valuation and compatibility formatters |
| Gross/net grouped risk and fines | Risk hooks only | Full fields/statuses (`0025`,`0028`) | Limits observed; internals incomplete | Inventory constraints only | Participant risk architecture | Exact simple limits | Multi-group engine risk and fine transactions |
| Tenders | None | Rich lifecycle/types (`0029`,`0030`) | Not current core evidence | No | No | None | Engine tender state, scheduling, allocation and private events |
| News | None | Ordered topics/routes/callbacks (`0031`) | Special-event target | No | Market-data transport only | None | Engine event store with public/private delivery |
| Assets, leases and conversions | None | Rich asset/lease/conversion model (`0032`-`0035`) | Not observed | No | No | None | Scenario asset subsystem and ledger postings |
| OTC and trade-at-settle | None | Rich client contracts (`0013`,`0014`) | Not observed | No | Participant order lifecycle concepts only | None | Engine special-order policies after formula evidence |
| Multi-asset derivatives and fixings | Generic symbols only | Security taxonomy and fields (`0005`,`0038`-`0040`) | Single/multiple markets configurable | Formula libraries may help | Market-data DTOs | Generic instrument IDs only | Instrument-specific packages composed by engine; no empty modules |
| Mark-to-market, settlement, interest and dividends | Fees/trades only | Transaction taxonomy and cadence (`0036`-`0038`) | Scoring/results target | Participant P&L estimates | Local positions | Trade cash/holding projection only | Exact event-driven ledger and valuation policy |
| Scoring, rankings and termination | None | Metrics and stop state; equations unresolved (`0043`,`0044`) | Results/leaderboard observed; equations under translation | Agent P&L only | No venue score | None | Versioned engine score/termination modules with golden evidence |
| REST compatibility | None | Exact route table (`0048`) | NBC HTTP/WebSocket compatibility target | Existing RIT HTTP adapter model | gRPC/Python compatibility | Native tRPC Worker only | External adapters call one engine API; no REST router in Worker target |
| Excel RTD/VBA compatibility | None | Exact API/topic surface (`0049`-`0052`) | None | None | None | None | Optional native Windows adapter over engine query/subscription data |
| Desired/live reconciliation | None | OTC/order client tables imply local views; exact algorithm unresolved | Client protocol only | Core strategy behavior | Strong participant-side evidence | Planned package only | Participant reconciliation package remains outside venue authority; built-in agents use an engine-owned bounded planner |
| Duplicate/out-of-order reports | None | Ordered updates observed, failure behavior unresolved | WebSocket ordering unresolved | Reconnect/stale quote concerns | Deferred fill observed; broader behavior unresolved | Canonical idempotency at origin only | Participant reconciliation package; engine command idempotency remains separate |
| Origin commit/idempotency | Sequencer helpers only | Not proved | Not proved | Not venue-side | Not venue-side | Implemented vertical slice with optimistic origin version | Preserve commit-before-ack and extend snapshot scope |
| Native/Wasm deterministic agreement | Upstream builds required | Windows-only reference | Bunting-added requirement | Floating model risk | Portable core target | Current workspace target check required | Golden state hashes and vectors for every deterministic subsystem |

## Current architectural mismatch

ADR 0018 supersedes ADR 0014's durable `orderbook-v1`/`nbc-v1` selection, and ADR 0019 makes the OrderBook-rs adapter integral to the central engine package. The current `packages/orderbook` and separate NBC matching loop are transitional implementation state. ADR 0014's market-versus-participant authority boundary and ADR 0017's NBC provenance rules remain binding.

The current code is therefore plumbing for several rows, not parity: it has an OrderBook-rs adapter, a basic ledger/risk/origin transaction path, a partial NBC config/run/matching port, and QUARCC compatibility records. It lacks the unified kernel, broad RIT state model, agents, tenders/news/assets, complete valuation, full-state recovery, and feature-to-test coverage gate.
