# QUARCC compatibility instructions

Preserve the public `quarcc.v1` service names, field meanings, and enum discriminants without importing unlicensed implementation text. Keep the default crate WASM-safe and transport-neutral. Legacy floating-point fields stay at this compatibility boundary and never enter canonical Bunting accounting without checked fixed-point conversion.
