# tRPC wire package instructions

Keep this package sans-I/O and market-neutral. It implements only the bounded identity-JSON HTTP subset frozen in `schemas/trpc/bunting.v1.json`; extensions must reject explicitly.
