# risk-engine instructions

Implement deterministic pre-trade and run-level risk with stable reason codes. Risk checks consume immutable views and emit decisions; they do not call external storage or services.
