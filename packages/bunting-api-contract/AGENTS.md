# API contract package instructions

This package owns the Rust procedure types and deterministic contract descriptor. Keep browser parsing in `browser-wire`, FIX parsing in `simfix-wire`, market behavior outside this package, and all externally wide integers as validated decimal strings.
