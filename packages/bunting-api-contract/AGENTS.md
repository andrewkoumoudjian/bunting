# API contract package instructions

This package owns the Rust procedure types and deterministic contract descriptor. Keep transport parsing in `trpc-wire`, market behavior outside this package, and all externally wide integers as validated decimal strings.
