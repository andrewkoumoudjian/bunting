# strategy-loader instructions

This minimal TypeScript service owns only Dynamic Worker Loader calls. User code receives no secrets or direct storage, uses denied global egress, explicit limits, bounded input/output, and immutable source hashes.
